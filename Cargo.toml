[package]
name = "project-name"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.8"
rand = "0.8"
ron = "0.7"
serde = { version = "1.0", features = ["derive"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3